version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    cooldown:
      default-days: 10
    schedule:
      interval: monthly
    groups:
      github-actions:
        patterns: ["*"]
